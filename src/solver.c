#include <stdio.h>
#include <stdlib.h>
#include <stddef.h>
#include "radix.h"

/**
Solve a grid.

Parameters
----------
grid: The grid to solve. The grid must be squared a matrix of strings.
length: The size of the matrix.
returnLength: A pointer to a size_t that will be modified by the function to be
			  the size of the returned array.

Returns
-------
An array of strings containing the words found.
*/
char** solve(char*** grid, size_t gridLength, size_t* returnLength){
	
}

int main(){
   
}
